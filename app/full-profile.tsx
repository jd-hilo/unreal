import { ProfileShell } from '@/app/(tabs)/profile';

/** Long form: school, work, money, premium, etc. Linked from Twin insights. */
export default function FullProfileScreen() {
  return <ProfileShell mode="full" />;
}
